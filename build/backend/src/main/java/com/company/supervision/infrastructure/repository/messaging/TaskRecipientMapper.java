package com.company.supervision.infrastructure.repository.messaging;import com.baomidou.mybatisplus.core.mapper.BaseMapper;import com.company.supervision.domain.model.messaging.TaskRecipient;import org.apache.ibatis.annotations.*;import java.util.*;@Mapper public interface TaskRecipientMapper extends BaseMapper<TaskRecipient>{@Select("SELECT * FROM supervision_task_recipient WHERE task_id=#{taskId} ORDER BY id")List<TaskRecipient>byTask(Long taskId);@Delete("DELETE FROM supervision_task_recipient WHERE task_id=#{taskId}")int deleteByTask(Long taskId);}