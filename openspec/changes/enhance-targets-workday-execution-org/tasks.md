# 实施任务：增强企微目标、国家工作日、执行可观测性与组织视图

## 0. 基线与兼容

- [ ] 0.1 使用 OpenSpec CLI 校验变更状态、工件格式和 apply 指令。
- [ ] 0.2 为当前机器人、单目标任务、调度预览、执行日志和组织同步补回归测试。
- [x] 0.3 定义旧 `robotId`、单 `webhookId`、旧机器人名称和旧执行记录的兼容期限与迁移标记。
- [x] 0.4 统一执行状态字典，先修复执行日志状态筛选成功/失败值反转。

## 1. 群与消息推送管理

- [x] 1.1 新增逻辑群表并扩展 Webhook 表的 group_id、push_name、system_code、更新时间字段。
- [x] 1.2 编写幂等迁移：旧名称迁入推送名称，创建待补群名分组，保留 RobotId 映射和加密配置。
- [x] 1.3 重构机器人应用服务为群与消息推送管理，系统自动生成兼容标识。
- [x] 1.4 重构管理页面：群名、推送名称、Webhook、状态、测试时间；RobotId 不再由用户必填。
- [x] 1.5 实现按群分组的“群名 · 推送名称”选择器 DTO，继续保护 Webhook 密文和 key。
- [ ] 1.6 增加群/推送 CRUD、迁移、停用、测试发送和敏感字段保护测试。

## 2. 多目标群投递

- [x] 2.1 将消息定义升级为 `webhookIds`，兼容读取单个 `webhookId`。
- [x] 2.2 任务向导目标改为多选、按群分组，并实现同群多推送重复触达警告与确认。
- [x] 2.3 保存任务时批量验证目标存在、启用且可解密。
- [x] 2.4 执行时按目标扇出，建立目标级幂等键、限流和独立 Delivery。
- [x] 2.5 实现 SUCCESS、PARTIAL_SUCCESS、FAILED、RUNNING 聚合及默认“失败后继续其他目标”。
- [ ] 2.6 实现只重试失败且可重试目标，不重发已成功目标。
- [ ] 2.7 增加多目标混合结果、同群重复、目标停用和重试幂等测试。

## 3. 国家法定工作日

- [x] 3.1 新增年度日历覆盖表、例外日期表和导入审计字段。
- [x] 3.2 定义并录入首批受支持年份的数据包，附国务院通知来源、版本和校验和。
- [x] 3.3 实现 `NationalWorkdayCalendar` 的 WORKDAY/HOLIDAY/UNKNOWN 判定。
- [x] 3.4 增加 WORKDAY 规则校验、保存、每日候选 Cron 编译和未来五次预览。
- [x] 3.5 在运行前加入相同日历守卫，记录执行、节假日跳过或日历缺失原因。
- [ ] 3.6 在系统设置增加日历覆盖年份、来源、更新时间、缺失告警和受控导入。
- [ ] 3.7 增加普通工作日、法定假日、周末调休、跨年缺失和预览一致性测试。

## 4. 执行日志与可观测性

- [x] 4.1 扩展执行与投递快照字段，定义历史数据不完整标记。
- [x] 4.2 执行开始和目标发送时保存任务名、消息摘要、群名、推送名和触发/日历决策快照。
- [x] 4.3 实现统一执行列表 DTO、详情 DTO、分页筛选和目标级聚合查询。
- [x] 4.4 实现企微错误码/异常到业务可读原因的翻译及脱敏技术详情。
- [x] 4.5 重构执行日志列表和详情抽屉，展示耗时、目标成功数、消息摘要和逐目标时间线。
- [x] 4.6 仪表盘复用执行摘要契约和状态字典。
- [ ] 4.7 增加任务改名、消息修改、部分成功、旧记录、权限和脱敏测试。

## 5. 组织人员管理

- [x] 5.1 扩展人员的性别、同步状态、脱敏错误、主要部门等字段并迁移现有数据。
- [x] 5.2 实现部门树直接/递归人数统计、部门详情和状态返回。
- [x] 5.3 实现成员按部门、姓名/企微账号、成员状态、性别、同步状态分页查询。
- [x] 5.4 同步服务映射新增字段并维护逐成员状态，部分失败时保留历史数据。
- [x] 5.5 按附件重构组织页面：同步概览、筛选、组织树、成员列表、部门详情和成员详情。
- [x] 5.6 统一“成员状态/在职状态”和“企微账号”术语，禁止手工新增及登录账号生命周期联动。
- [ ] 5.7 增加树计数、组合筛选、多部门、离职、部分同步失败、脱敏和无手工新增测试。

## 6. 集成、部署与验收

- [x] 6.1 更新一键部署脚本和迁移备份说明，确保新年度日历数据进入发布包。
- [x] 6.2 执行后端 Docker Maven 测试、前端生产构建和迁移测试。
- [ ] 6.3 在服务器升级并验证迁移、五容器健康、认证及核心查询接口。
- [ ] 6.4 使用至少两个群和同群两个推送完成多目标、重复警告、部分成功及目标级重试冒烟。
- [ ] 6.5 验证法定假日、周末调休工作日、日历缺失和未来时间预览。
- [ ] 6.6 使用真实企微组织数据验收组织树人数、筛选、详情、同步状态和日志。
- [ ] 6.7 验收执行日志的任务/内容快照、可读错误、技术详情权限和仪表盘口径。
- [x] 6.8 更新产品、技术、部署、年度日历维护和用户验收文档。
