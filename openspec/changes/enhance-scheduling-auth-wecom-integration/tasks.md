# 实施任务：增强调度、账号体系与企业微信集成

> 状态：实现完成，等待受限环境与真实企微联调验收。实施时按阶段提交，每阶段完成后更新本文件和项目文档。

## 0. 基线与迁移准备

- [x] 0.1 增加数据库迁移机制，避免继续只依赖单个初始化 SQL。
- [x] 0.2 为当前任务、调度和企微发送链路补充回归测试。
- [x] 0.3 修复前端定时值 `1/2` 不一致和后端固定 Markdown 的基线缺陷。
- [x] 0.4 定义统一枚举、错误码、审计字段和敏感字段脱敏规则。

## 1. 账号认证与权限

- [x] 1.1 创建 account、role、account_role、login_audit 表及领域模型。
- [x] 1.2 实现密码哈希、登录失败锁定、会话或 JWT 签发与撤销。
- [x] 1.3 实现首次登录 `CHANGE_PASSWORD_ONLY` 状态和强制改密接口。
- [x] 1.4 实现账号 CRUD、停启用、重置密码和角色分配接口。
- [x] 1.5 实现账号与人员的可选一对一绑定约束。
- [x] 1.6 增加登录页、首次改密页、账号管理页和路由守卫。
- [ ] 1.7 补充认证、越权、锁定、重置及会话撤销测试。
- [x] 1.8 Resolve the login display name from the bound organization person first and test fallback behavior.

## 2. 企微配置与组织同步

- [x] 2.1 创建 wecom_config、department、person、person_department、sync_log 表。
- [x] 2.2 实现敏感配置加密、掩码读取和配置修改审计。
- [x] 2.3 实现企微 access_token Redis 缓存和安全刷新。
- [x] 2.4 实现企微配置查询、保存和连通性/权限验证接口。
- [x] 2.5 实现部门、人员、人员多部门关系的幂等全量同步。
- [x] 2.6 实现“完整快照成功后才停用缺失对象”的同步收敛逻辑。
- [x] 2.7 实现同步锁、同步统计、错误明细和同步日志查询。
- [x] 2.8 增加系统设置、组织树、人员列表和同步记录页面，禁止手工新增人员。
- [ ] 2.9 补充重复同步、部分失败、离职停用和配置权限不足测试。
- [x] 2.10 Decouple shared organization queries from administrator-only synchronization and logs.

## 3. 企微消息领域

- [x] 3.1 创建 webhook、message_template、task_recipient、message_delivery 表。
- [x] 3.2 将现有机器人迁移为加密的 GROUP_WEBHOOK 配置。
- [x] 3.3 定义统一消息模型、消息类型校验器和渠道适配器接口。
- [x] 3.4 实现群消息 text、markdown、markdown_v2、news、template_card。
- [x] 3.5 实现图片和文件上传及 media_id 生命周期处理。
- [x] 3.6 实现 voice 和图文展示模板卡片，或明确关闭对应功能开关。
- [x] 3.7 实现企微应用消息按人员、部门和标签发送。
- [x] 3.8 实现 @指定人员、@所有人以及 Markdown v2 能力限制。
- [x] 3.9 实现投递幂等、每 Webhook 每分钟 20 条限流、错误分类和重试。
- [x] 3.10 实现消息预览、测试发送和投递记录查询接口。
- [x] 3.11 增加组织选择器、结构化消息编辑器和消息预览。
- [ ] 3.12 补充八类群消息契约测试和应用消息真实联调用例。

## 4. 高级调度

- [x] 4.1 扩展 schedule 表并新增 trigger 表，定义版本化 rule_json。
- [x] 4.2 实现一次、分钟/小时、每天、每周、每月和高级 Cron 规则校验。
- [x] 4.3 实现规则到 Quartz Trigger/Calendar 的编译器。
- [x] 4.4 实现时区、开始/结束、工作日、misfire 和重叠策略。
- [x] 4.5 实现执行超时、固定/指数退避重试与任务幂等键。
- [x] 4.6 实现未来五次执行时间预览接口。
- [x] 4.7 将任务编辑重构为包含调度步骤的向导，默认隐藏 Cron。
- [x] 4.8 迁移现有手动/Cron 任务并验证重启恢复。
- [ ] 4.9 补充夏令时边界、月末、错过执行、任务重叠和重启恢复测试。

## 5. 集成、文档与验收

- [x] 5.1 将结构化 schedule、recipients、messageDefinition 接入任务 CRUD。
- [x] 5.2 完成旧任务节点和旧机器人数据兼容、迁移与回滚脚本。
- [ ] 5.3 使用 Docker 执行后端 compile/test、前端 build 和现有冒烟测试。
- [ ] 5.4 在测试企业完成通讯录、应用消息、群消息和 @能力真实联调。
- [x] 5.5 完成安全检查：密码、Secret、Webhook、Token 和日志均不泄漏。
- [x] 5.6 更新产品、技术、部署、配置、接口和用户验收文档。
- [ ] 5.7 根据四项 capability spec 逐条验收并记录证据。

## 执行顺序

`0 → 1 → 2 → 3 → 4 → 5`。消息领域依赖组织同步；任务向导整合依赖消息与调度模型。若需要降低首期范围，可在不破坏统一模型的前提下推迟 3.5、3.6 和法定节假日日历实现。