# 实施任务：修复任务目标群选择

## 1. 生产结构确认与迁移

- [ ] 1.1 只读导出生产 `supervision_person`、`supervision_department`、`supervision_person_department` 的列、索引和外键元数据。
- [x] 1.2 定义旧字段到当前模型的映射、默认租户键及重复/空值处理规则。
- [x] 1.3 新增下一版本 Flyway 兼容迁移，只新增和回填，不删除旧列或重建主键。
- [ ] 1.4 增加从旧结构和当前结构升级的迁移集成测试，验证重复执行安全性与数据保留。

## 2. 后端目标群契约

- [x] 2.1 明确加密 Webhook 配置为可发送目标的有效性事实来源，并实现非敏感选择器 DTO/查询。
- [x] 2.2 过滤禁用、缺失或不可解密配置，保持机器人名称与稳定 `webhookId` 的兼容映射。
- [x] 2.3 在任务创建和更新时校验目标存在且启用，禁止静默替换。
- [x] 2.4 在任务执行时对失效目标生成脱敏、可审计的投递失败记录。
- [ ] 2.5 增加选择器过滤、任务校验、执行一致性和敏感字段保护测试。

## 3. 前端任务向导

- [x] 3.1 将人员与目标群请求拆为独立加载状态，移除单个 `Promise.all` 的共同失败路径。
- [x] 3.2 打开/重开任务向导及进入消息触达步骤时刷新目标群，并提供手动重试。
- [x] 3.3 为目标群增加加载、空列表、局部失败及已保存目标失效的明确提示。
- [ ] 3.4 增加“人员失败但群成功”“群失败但人员成功”“新增机器人后刷新可见”的前端测试。

## 4. 部署与验收

- [x] 4.1 更新部署文档及本地一键更新脚本，部署前备份数据库并在失败时停止。
- [x] 4.2 执行后端 Docker 构建测试和前端生产构建。
- [ ] 4.3 在服务器验证 Flyway、人员/部门接口、目标群选择器及认证状态。
- [ ] 4.4 完成“选择目标群 → 保存任务 → 测试发送 → 核对投递记录”的端到端冒烟。
- [x] 4.5 将验证证据、风险和未完成事项同步到项目 `doc/`。
