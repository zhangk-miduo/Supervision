# README 现状重写报告

> **版本**：v1.0 | **日期**：2026-07-23 | **状态**：已落地

## 执行摘要

本次根据仓库当前代码、数据库迁移、前端路由、Docker Compose 编排和近期实现记录，重写根目录 `README.md`。新 README 已从早期命名讨论和规划稿调整为面向使用者与开发者的项目入口，明确当前已经实现的能力、部署方式、验证路径及已知边界。

## 用户要求

根据项目当前设计和实现情况，重新编写 README 文档内容。

## 重要证据与结论

- 前端已实现仪表盘、任务、机器人、执行日志、登录改密、账号、组织人员和企业微信设置页面。
- 后端已实现任务、执行、账号认证、组织同步、企业微信消息、调度预览和工作日历等接口。
- 数据库迁移已覆盖账号认证、企业微信组织与消息、高级调度、结构化任务、管理员角色以及账号数据范围与公开机器人。
- 当前任务调度支持手动、单次、每日、工作日、每周、每月、间隔和 Cron。
- 当前 Compose 仅对外映射 Web 服务的 `8002:80`，API 通过 Nginx 的 `/api` 反向代理访问；旧文档中的 `http://localhost` 与 RabbitMQ 控制台直连描述不适合作为当前 README 入口信息。
- 后端为 Spring Boot 3.2.2 / Java 17 单模块 DDD 分层实现，不是旧 README 所描述的多 Maven 模块结构。

## 完成的修改

- 删除旧 README 中的产品命名讨论、仓库命名建议、重复嵌套 README 草稿和已过期的模块规划。
- 新增当前能力、系统架构、技术栈、快速开始、首次使用、本地开发与验证、项目结构和已知边界。
- 将配置说明与 `.env.example` 对齐，不记录任何真实密钥。
- 将访问地址与 `docker-compose.yml` 的 `8002` 端口映射对齐。
- 区分当前已实现能力与尚未提供的钉钉、飞书、邮件、AI 节点及多实例高可用调度。

## 影响范围

- `README.md`
- `doc/report.supervision.readme-rewrite.md`

未修改业务代码、运行配置或数据库结构。

## 风险与开放问题

- `DEPLOYMENT.md` 中仍存在与当前 Compose 端口和服务暴露方式不一致的旧描述，后续应单独校准。
- README 的能力描述以 2026-07-23 工作区代码为准；后续新增渠道、调度模式或权限规则时需要同步维护。
- 工作区在本次任务开始前已有未提交改动，本次未覆盖或整理这些用户改动。

## 验证建议

- 检查 README 中的命令、相对链接及访问端口。
- 执行前端生产构建。
- 通过 `docker compose build api` 验证 Java 17 后端编译和测试。
- 完整环境启动后执行“登录 → 创建任务 → 立即执行 → 查询执行日志”的冒烟流程。
